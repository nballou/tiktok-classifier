[project]
name = "tiktok-classifier"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pandas",
    "pyarrow",
    "openai>=1.0",
    "httpx",
]

[project.optional-dependencies]
hpc  = ["vllm"]
eval = ["datasets"]
